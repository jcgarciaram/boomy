Local development:

Need local Dynamo installed
    - As part of Dynamo, need Java JDK 9 installed.

Environment Variables that need to be defined:
    - AWS_REGION
    - 


TODO:
    - Build Resident handler for completely new users that are talking through chat.
    - Will initialize Resident with just ID and Conversation struct and return token
    - Complete GetResponse